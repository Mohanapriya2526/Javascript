//5)Create an new array, it need to have element from numbers array multiple of three
    let number = [35, 14, 70, 49, 21, 56, 7, 42, 28, 63, 49];
    let num = number.map((num)=>num*3);
    console.log(num);
