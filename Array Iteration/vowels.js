const name ="Mohanapriya S";
const numberOfVowels =vowels(inputString);
str = str.toLowercase();
const vowels = ["a","e","i","o","u"]
let count = 0;

function vowels(str){
    for (let i=0; i < vowels.length; i++ )
    {
        if (vowels.includes(str(i))){
            count++;
        }
      }
        return count;
    }
  console.log(inputString)
    
